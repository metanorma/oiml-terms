# Task 4: Build and test

Run the concept-browser build and verify the output.

## Steps
1. Install npm dependencies (link concept-browser)
2. Run `npx concept-browser build` with proper env vars
3. Verify `dist/` output has expected structure
4. Test locally if possible

## Environment variables
- `BASE_PATH=/oiml-terms/`
- `DATASET_SOURCE_G18` pointing to `g18-glossarist/` directory

## Dependencies
- Tasks 1, 2, 3 all complete
